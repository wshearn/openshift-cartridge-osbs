div.large-3.columns.panel
    h3 Navigation
    ul.side-nav
        each item in index
            if (item.active == true)
                li.active
                    a(href="#{item.url}")
                        | #{item.title}
            else
                li
                    a(href="#{item.url}")
                        | #{item.title}